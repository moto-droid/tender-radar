{
  "name": "tender-radar",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node --check app.js && node --check enhancements.js && node --check intelligence.js && node --check subscription.js"
  },
  "dependencies": {
    "@netlify/blobs": "^10.0.9",
    "cheerio": "^1.1.2",
    "nodemailer": "^9.1.1"
  },
  "devDependencies": {
    "@netlify/functions": "^4.2.5"
  },
  "engines": {
    "node": ">=20"
  }
}
